# 猎人与狼 (Hunter & Wolf)

一个基于 React + TypeScript + PWA 的策略棋盘游戏，猎人（3枚）对战狼群（15枚）。

## 🎮 游戏规则

### 棋盘
- 5×5 交叉点阵（共 25 个位置）
- **猎人（3枚）**：初始位于底部第一排的正中间三个点
- **狼（15枚）**：初始占满上部前三排所有位置

### 移动规则
- **普通移动**：双方棋子每次只能沿直线（上、下、左、右）移动一格至相邻的空位
- **猎人特技（隔空射杀）**：当猎人与某只狼在同一直线上，且两者中间恰好有一个空格时，猎人可以跳过空格直接移动到该狼的位置并将狼移除

### 胜负判定
- **猎人胜**：当棋盘上的狼被削减至 3 只或以下
- **狼群胜**：狼群通过合围，使得 3 个猎人全部无法移动

## 🛠️ 技术栈

- **框架**: React 18 + Vite
- **UI**: Tailwind CSS（响应式设计）
- **动画**: Framer Motion
- **状态管理**: Zustand
- **AI**: Minimax + Alpha-Beta 剪枝（Web Worker 后台计算）
- **PWA**: vite-plugin-pwa（支持离线运行和"添加到桌面"）

## 📁 项目结构

```
src/
├── components/       # React 组件
│   ├── Board.tsx    # 棋盘组件
│   └── GameInfo.tsx # 游戏信息面板
├── lib/            # 游戏核心逻辑
│   ├── types.ts     # 类型定义
│   ├── board.ts     # 棋盘初始化和位置计算
│   ├── moves.ts     # 移动验证和执行
│   ├── snipe.ts     # 猎人射杀逻辑
│   ├── gameState.ts # 胜负判定
│   ├── aiMoves.ts   # AI 移动生成
│   └── evaluation.ts # 评估函数
├── store/          # 状态管理
│   ├── gameStore.ts # 游戏状态 (Zustand)
│   └── aiStore.ts  # AI Worker 管理
├── workers/        # Web Workers
│   └── aiWorker.ts # Minimax AI
├── hooks/
│   └── useVibration.ts # 触觉反馈
├── App.tsx
└── main.tsx
```

## 🚀 快速开始

### 安装依赖
```bash
npm install
```

### 开发模式
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 预览生产构建
```bash
npm run preview
```

## 🎯 核心功能

### 1. 棋盘状态管理
- 使用一维数组 `Array(25)` 表示 5×5 棋盘
- Zustand 管理全局游戏状态
- 支持游戏重置和历史记录

### 2. 移动验证
- 相邻位置检测（上下左右）
- 边界检查
- 猎人射杀规则验证

### 3. AI 算法
- **算法**: Negamax + Alpha-Beta 剪枝
- **搜索深度**: 自适应 4-6 层（根据狼的数量）
- **评估函数**:
  - 狼数量计分（权重 1000×）
  - 狼群紧凑度（权重 10×）
  - 猎人机动性惩罚（权重 5×）
  - 狼群推进度（权重 3×）
  - 围堵潜力（每只相邻狼 +15）
- **性能**: Web Worker 后台计算，避免 UI 卡顿

### 4. PWA 功能
- Service Worker 自动生成
- 离线运行支持
- "添加到主屏幕"功能
- 响应式移动端优化

### 5. 用户体验
- 触觉反馈（Web Vibration API）
- 移动优先设计
- 流畅的交互动画
- 视觉反馈（高亮、阴影等）

## 🎨 AI 策略说明

### 评估函数设计
```typescript
狼方分数 = 狼数量×1000
          - 狼群距离之和×10
          - 猎人机动性×5
          + 狼群平均行数×3
          + 相邻猎人数量×15
```

### 自适应深度
- 早期（狼 > 12）：深度 6
- 中期（狼 6-12）：深度 5
- 后期（狼 ≤ 6）：深度 4

### 移动排序
1. 猎人射杀优先
2. 向中心推进的移动
3. 随机平局避免重复

## 📱 移动端优化

- 响应式 Grid 布局
- 触摸友好的点击区域
- 移动端适配的 AI 超时（800ms）
- 触觉反馈增强游戏体验
- 深色模式支持

## 🔧 配置说明

### PWA 配置
配置文件：`vite.config.ts`
```typescript
VitePWA({
  registerType: 'autoUpdate',
  manifest: {
    name: '猎人与狼',
    short_name: '猎人与狼',
    // ...
  }
})
```

### Tailwind 配置
配置文件：`tailwind.config.js`
```javascript
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      gridTemplateColumns: { '5': 'repeat(5, minmax(0, 1fr))' },
      gridTemplateRows: { '5': 'repeat(5, minmax(0, 1fr))' },
    },
  },
}
```

## 📝 待完成功能

- [ ] Framer Motion 动画增强（已委托 frontend-ui-ux-engineer）
- [ ] PWA 离线模式测试
- [ ] 拖拽操作支持
- [ ] 游戏回放功能
- [ ] AI 难度选择
- [ ] 音效支持

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT

---

**技术实现**：基于 Oracle 架构指导的 Minimax AI，使用 Negamax 算法和自适应深度策略。
